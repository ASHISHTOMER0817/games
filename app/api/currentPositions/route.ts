import { NextResponse, NextRequest } from "next/server";
import { Server } from "socket.io";


export function GET(){

      // try{
      //       const io = new Server()
      //       io.on("connection", (socket)=>{})
      //       // mongoose
      // }
}